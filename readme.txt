Steps Starting App:

1. Jalankan perintah `php artisan migrate --seed` untuk migrasi dan seeding database.
2. Jalankan `npm run dev` untuk membangun aset frontend.
3. Jalankan `php artisan serve` untuk menjalankan server lokal.
4. Buka aplikasi web di [http://127.0.0.1:8000](http://127.0.0.1:8000).